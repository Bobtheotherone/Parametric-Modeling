# This directory holds the SQLite artifact registry.
# The artifacts.sqlite database will be created here by ArtifactRegistry.
# The database is derived data (rebuildable from manifests) and should not be committed.
